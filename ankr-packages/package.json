{
  "name": "ankr-packages",
  "version": "1.0.0",
  "private": true,
  "description": "ANKR Reusable Packages - Enterprise-grade utilities for fintech applications",
  "author": "ANKR Technologies",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "publish:all": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "@types/node": "^20.19.30",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
